"use strict";

var game = game || {};

game.score = function(specspec){
    var that = {};
    var element = spec.element;

    that.report = function() {
    }

    that.increment = function() {
    }

    return that;
};